# Harbor Docs

Documentation site for the Harbor SDK, built with [Docusaurus](https://docusaurus.io/) v3.

## Local setup

Requires Node.js 20 (`nvm use` reads `.nvmrc`).

```bash
pnpm install
pnpm start
pnpm build
```
