{
  "defaults": {
    "standard": "WCAG2AA",
    "runners": ["axe"],
    "timeout": 30000,
    "chromeLaunchConfig": {
      "args": ["--no-sandbox", "--disable-dev-shm-usage"]
    },
    "hideElements": ".theme-doc-version-badge, .mermaid",
    "ignore": ["color-contrast"]
  }
}
